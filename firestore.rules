rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TR-ID: Allow all read/write for 30 days for development
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2024, 8, 1);
    }
  }
}
